# ========================================
# CLIENT-SIDE ENVIRONMENT VARIABLES
# (Exposed in browser bundle - for demo/testing only)
# ========================================

# Google Gemini API Key (client-side, for demo mode)
# Get your API key from: https://aistudio.google.com/app/apikey
# ⚠️ WARNING: Do NOT use this for PHI/production! Use server-side key instead.
NEXT_PUBLIC_GEMINI_API_KEY=your_api_key_here

# Enable/disable ingest endpoint
NEXT_PUBLIC_INGEST_ENABLED=true

# ========================================
# SERVER-SIDE ENVIRONMENT VARIABLES
# (Secure, never exposed to client)
# ========================================

# Google Gemini API Key (server-side, for production iOS workflow)
# This key is used by /api/ingest endpoint and never exposed to client
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_server_api_key_here

# Ingest Token (Bearer authentication for /api/ingest)
# Generate a strong random token:
#   - macOS/Linux: openssl rand -base64 32
#   - Or use: https://generate-secret.vercel.app/32
INGEST_TOKEN=your_long_random_token_here

# Scribe Webhook URL (optional)
# Your AI scribe app endpoint that receives completed transcripts
# Example: https://your-scribe-app.example.com/api/webhooks/transcript
SCRIBE_WEBHOOK=https://your-scribe-webhook-url.example.com/webhook

# ========================================
# NOTES
# ========================================
#
# DEMO MODE vs PRODUCTION MODE:
# - Demo Mode: Set NEXT_PUBLIC_GEMINI_API_KEY (client-side, not for PHI)
# - Production Mode: Set GEMINI_API_KEY + INGEST_TOKEN (server-side, secure)
#
# For iOS Shortcut integration:
# 1. Set GEMINI_API_KEY (server-side)
# 2. Set INGEST_TOKEN (share this with your iOS Shortcut)
# 3. Optionally set SCRIBE_WEBHOOK for automatic forwarding
#
# HIPAA/BAA Compliance:
# - Current setup uses Gemini AI Studio (NOT HIPAA-compliant)
# - For HIPAA compliance, migrate to Vertex AI on Google Cloud with BAA
# - See UPDATES.md for Vertex AI migration instructions
